function EditingTools({ videoPath }) {
  return (
    <div>
      <h3>Editing Tools for {videoPath}</h3>
      <p>Trim, shuffle, etc. will go here.</p>
    </div>
  );
}

export default EditingTools;